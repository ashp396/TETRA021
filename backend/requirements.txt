# Pinned to versions that publish prebuilt wheels for Python 3.13. If a
# future pip install still fails on one specific package, it is almost
# always ctranslate2 (a dependency of faster-whisper, used for voice
# transcription) since ML wheel builds are usually the slowest to catch
# up on a new Python release. See backend/README.md for the fallback.
faster-whisper>=1.1.0
fastapi==0.115.5
uvicorn[standard]==0.32.0
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
pydantic==2.9.2
pydantic-settings==2.5.2
email-validator==2.2.0
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
python-multipart==0.0.12
groq==0.11.0
pypdf==5.0.1
pdfplumber==0.11.4
python-docx==1.1.2
python-pptx==1.0.2
openpyxl==3.1.5
reportlab==4.2.5
edge-tts==6.1.19
httpx==0.27.2
python-dotenv==1.0.1
